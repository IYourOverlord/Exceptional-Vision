# PROGRESS.md — состояние проекта Exceptional Vision

> **Для нейросети, продолжающей эту работу:** прочитай этот файл целиком
> перед тем как что-либо менять. Он держит соответствие с
> `08_roadmap_milestones.md` из папки спецификаций, но фиксирует
> **фактическое** состояние кода, а не то, что "должно быть по плану".
> Если этот файл противоречит комментариям в коде — верь коду и обнови
> этот файл.

Обновлено: этапы 1–3 реализованы, протестированы и **связаны друг с
другом** через `LodPipeline`. Этап 4 (базовый GPU-пайплайн) подключён к
жизненному циклу мода и прогнан в реальном Minecraft/LWJGL — 8 багов
найдено и исправлено за предыдущие сессии (двойное смещение в
`lod_quad.vert`, несогласованное GL-состояние, не-camera-relative
frustum-тест, переполнение `int` в `GreedyMesher.wallBetween`,
отсутствие нижнего порога дистанции для LOD, файлы регионов навсегда
терялись при гонке с фоновым прегенератором чанков, осиротевшие узлы
реально рисовались поверх актуальных, вода рисовалась как суша). См.
разделы «Сессия — первый реальный прогон» / «второй реальный прогон»
ниже для полной хронологии.

**Эта сессия:** закрыт последний открытый пункт из «Известные
пробелы» — компактор `nodes.bin` (пробел 7). `CacheCompactor` теперь
компактирует и узлы, и квады (`compactNodes`/`compactQuads`, публичный
`compact` запускает оба в правильном порядке), подключён к жизненному
циклу через `ExceptionalVisionClient.stopPipeline` (запускается в
фоновом потоке при выгрузке измерения, не блокирует рендер-поток) —
см. «Компактор `nodes.bin`/`quads.bin`» ниже для деталей и того, как
это соотносится с уже существующим фиксом Бага 7 (который решал ту же
проблему на уровне загрузки в GPU, но не трогал сам файл на диске).

> ✅ **Баг 9 — LOD рисовался рваными диагональными полосами с дырами —
> ПОДТВЕРЖДЁН ЗАКРЫТЫМ В ИГРЕ.** Хронология: `CacheCompactor` снят с
> подозрения → построчная сверка GPU-пайплайна не нашла ошибок
> индексации → диагностика локализовала причину в нестыкованных швах
> между соседними узлами разных LOD-уровней → реализована и автономно
> проверена (6 репро-кейсов) стыковка швов между узлами ОДНОГО уровня
> внутри региона (`GreedyMesher.meshBoundaryAlongX/Z` +
> `LodBuilder.stitchLevel`) → **игрок собрал JAR, создал новый мир,
> прогнал со сменой радиуса прогрузки 32→7 — на всех присланных
> скриншотах полос/дыр нет, логи чистые.** Формально швы между РАЗНЫМИ
> LOD-уровнями и между соседними РЕГИОНАМИ всё ещё не стыкуются (это не
> было целью данного фикса), но визуально регрессия закрыта полностью
> или почти полностью — если что-то всплывёт, это будет тот самый
> оставшийся кусок, не новый баг. См. «Баг 9» ниже, разделы «РЕЗУЛЬТАТ
> ДИАГНОСТИКИ», «Реализована стыковка швов» и «ПОДТВЕРЖДЕНО В ИГРЕ»,
> для полных деталей.
>
> **Следующая по приоритету задача этапа 4:** развести семантику
> `baseLodDistance` vs `lodRenderDistance` в `quad_cull.comp` (сейчас
> `baseLodDistance` берётся напрямую из `lodRenderDistance`, без
> отдельного смысла — вопрос баланса производительности/детализации,
> не корректности геометрии). После этого этап 4 можно считать
> стабильным и переходить к этапу 5.

> **Уточнение из прошлой сессии, актуально и сейчас:** класс
> `client/ExceptionalVisionClient.java` — wiring, который одна из
> ранних версий этого файла ошибочно описывала как отсутствующий —
> на самом деле уже существовал в репозитории. Урок на будущее (для
> следующей нейросети, читающей этот файл): при продолжении работы
> **сверяйся с фактическим деревом файлов, а не только с этим файлом**
> — сам файл явно предупреждает об этом в шапке.

---

## Сводная таблица

| Этап | Название | Статус | Комментарий |
|---|---|---|---|
| 0 | Юридическая/орг. база | ⚠️ частично | Gradle-проект (NeoForge 1.21.1) настроен и мод компилируется/грузится. **LICENSE и README отсутствуют** — надо добавить перед публикацией. |
| 1 | Независимое чтение мира | ✅ готово | `RegionFileReader`, `WorldDirectoryWatcher`, `RegionImportQueue`, `ChunkHeightData`. Юнит-теста на реальном мире Chunky нет (нет доступа к MC-рантайму офлайн). |
| 2 | LOD-структура и даунсемплинг | ✅ готово | `ColumnGrid`, `GreedyMesher`, `LodDownsampler`, `MaterialPalette`, `LodNode`, `LodBuilder`, `LodBuilderExecutor`. |
| 3 | Дисковый кэш | ⚠️ готово; версия про связь с Багом 9 ослаблена реальным тестом | Плюс новое: `appendRegion`/`patchRegion` — региональные операции без дублирования узлов при повторной обработке. `CacheCompactor.compactNodes` — компактор `nodes.bin`, закрывает пробел 7. Подключён к жизненному циклу (`ExceptionalVisionClient.stopPipeline`). **См. Баг 9** — визуальная регрессия LOD, причина не установлена; штатный `LodCacheWriter` не может создать сценарий, который бы её объяснил через компактор (проверено исполняемым тестом), приоритет расследования смещён на этап 4 (GPU). Отдельно найдена не связанная с Багом 9 хрупкость `compactNodes` при вручную побитом `index.json` — тихо дублирует данные вместо ошибки, не пофикшено. |
| **связка 1→2→3** | — | ✅ готово | `pipeline/LodPipeline.java` — закрывает пробел, который был явно отмечен в предыдущей версии этого файла. |
| 4 | GPU-пайплайн (базовый) | ⚠️ подключён, прогнан в игре, 9 багов найдены и исправлены (Баг 9 подтверждён закрытым в игре) — осталась семантика `baseLodDistance`/`lodRenderDistance` перед переходом на этап 5 | `render/` пакет + `client/ExceptionalVisionClient.java` (wiring) + `world/RegionImportQueue.java` + `cache/LodCacheData.java`. См. раздел «Сессия — первый реальный прогон» ниже и «Баг 9» для полной хронологии. |
| 5 | Полный GPU-driven путь | ❌ не начато | |
| 6 | Стыковка с ближним рендером | ❌ не начато | |
| 7 | Конфигурация и UX | ❌ не начато | `ExceptionalVisionConfig` — только скелет (2 поля). |
| 8 | Полировка и релиз | ❌ не начато | |

---

## Этап 1 — Независимое чтение мира (✅)

Без изменений с прошлой версии этого файла. Файлы:
`src/main/java/org/ex/exceptionalvision/world/{RegionFileReader,
RegionCoordinate, RegionHeader, RegionResult, ChunkHeightData,
WorldDirectoryWatcher, RegionImportQueue}.java`.

**Контракт, важный для следующих этапов:**
- `ChunkHeightData.topY(x,z) == Integer.MIN_VALUE` — колонка пуста.
- `ChunkHeightData.complete()` — `false` для чанков со статусом ниже `full`.
- **Данных о свете не извлекается** (см. «Известные пробелы»).

---

## Этап 2 — LOD-структура и даунсемплинг (✅)

Без изменений с прошлой версии. Файлы:
`src/main/java/org/ex/exceptionalvision/world/lod/{PackedQuad,
NodeData, ColumnGrid, GreedyMesher, LodDownsampler, LevelStats,
LodBuildResult, MaterialPalette, LodNode, LodBuilder,
LodBuilderExecutor}.java`.

**Ключевые решения, обязательные к соблюдению в этапе 4/5/6** (см. также
javadoc `GreedyMesher`/`LodBuilder`):
1. Сетка узла всегда 16×16 ячеек; ячейка на уровне `L` = `2^L` блоков.
   `worldX = node.minX + localX * 2^lodLevel` — **шейдер этапа 4 уже
   реализует это** (`lod_quad.vert`), сверено построчно с `GreedyMesher.java`.
2. Конвенция width/height в `packed1` (верх: X/Z в ячейках; стены:
   вдоль границы в ячейках, по вертикали — в блоках) — **тоже уже
   перенесена в `lod_quad.vert`** построчно.
3. Стены только на внутренних границах узла — швы между узлами не
   закрыты (задача этапа 6).
4. Downsampling берёт максимум высоты, не среднее.
5. `LodNode.dirty` не подключён ни к чему автоматически (см. пробелы).

---

## Этап 3 — Дисковый кэш (✅, дополнено)

Файлы: `src/main/java/org/ex/exceptionalvision/cache/`.

**Изменения с прошлой версии этого файла:**
- `RegionCacheEntry` расширен полями `nodeIndexStart`/`nodeCount` —
  теперь `index.json` знает, какой диапазон записей в `nodes.bin`
  принадлежит региону. Старые файлы `index.json` без этих полей
  читаются нормально (дефолт 0/0) благодаря `CacheIndex.optionalInt`.
- `markRegionProcessed` **удалён**, вместо него:
  - `LodCacheWriter.appendRegion(...)` — первая обработка региона ИЛИ
    повторная обработка, при которой количество узлов изменилось
    (например, раньше несгенерированные чанки наконец сгенерировались).
    Сдвигает `quadOffset` каждого узла на текущий глобальный размер
    `quads.bin` (узлы из `LodBuildResult` приходят с
    **регион-локальными** смещениями — см. `LodBuilder.flatten`).
  - `LodCacheWriter.patchRegion(...)` — повторная обработка региона С
    ТЕМ ЖЕ количеством узлов (обычный случай: содержимое чанков
    изменилось, топология — нет). Патчит существующие записи узлов на
    месте, аппендит свежие квады (старые становятся мусором —
    подбираются `CacheCompactor`, как и раньше).
  - `LodCacheWriter.findRegionEntry(...)` — публичный метод для
    проверки, есть ли у региона уже присвоенный диапазон узлов (нужен
    `LodPipeline`, чтобы решить `appendRegion` vs `patchRegion`).
- ~~**Известный пробел, специально задокументированный в javadoc
  `LodCacheWriter`:** компактится только `quads.bin`~~ — **закрыто в
  этой сессии**, см. «Компактор `nodes.bin`/`quads.bin`» ниже.

Всё протестировано (см. «Как это тестировалось»), включая
специальный тест на **отсутствие дублирования узлов** при
`patchRegion`.

---

## Компактор `nodes.bin`/`quads.bin` (✅, новое в этой сессии)

Файл: `src/main/java/org/ex/exceptionalvision/cache/CacheCompactor.java`.

Закрывает пробел 7 из предыдущих версий этого файла ("компактируется
только `quads.bin`, не `nodes.bin`"). Контекст важен: **это не то же
самое, что фикс Бага 7** (см. «Сессия — первый реальный прогон» ниже),
хотя они касаются одной и той же проблемы с двух разных сторон:

- **Фикс Бага 7** (`LodCacheData.of`/`filterToLiveRanges`, сделан
  раньше) — не даёт осиротевшим узлам **попасть на GPU и отрисоваться**.
  Работает в памяти, при каждой загрузке кэша, ничего не меняет на
  диске.
- **Этот компактор** — физически убирает осиротевшие узлы **с диска**,
  чтобы `nodes.bin` не рос бесконечно на очень долгоживущих мирах.
  Не заменяет фикс Бага 7 (тот всё ещё нужен — компактор запускается
  редко, раз в выгрузку измерения, а не при каждой загрузке), а
  дополняет его.

### Как это работает

`CacheCompactor` теперь умеет две независимые операции:

- `compactQuads(worldDir, dimensionId)` — без изменений в логике
  (это старый единственный метод `compact`, просто переименован):
  идёт по живым узлам в `nodes.bin`, копирует только те квады, на
  которые они ссылаются, в свежий `quads.bin`, патчит `quadOffset`
  каждого узла. Узлы не переупорядочиваются.
- `compactNodes(worldDir, dimensionId)` — новое: `index.json`'s
  `regionsProcessed` — это ровно тот же "live set" для `nodes.bin`,
  каким `nodes.bin` был для `quads.bin` (та же идея, на уровень выше).
  Каждая запись региона называет один смежный блок записей узлов,
  которым регион сейчас владеет — старые блоки (оставшиеся после
  `appendRegion` при смене числа узлов региона) нигде в `index.json`
  больше не упомянуты. Метод идёт по `regionsProcessed` в порядке
  возрастания старого `nodeIndexStart` (чтобы узлы одного региона
  остались смежными), копирует только живые блоки в свежий
  `nodes.bin`, и переписывает `index.json` с новыми смещениями.
- `compact(worldDir, dimensionId)` — публичная точка входа, запускает
  сначала `compactNodes`, потом `compactQuads` (порядок важен: после
  чистки узлов список, который проходит `compactQuads`, уже не
  содержит мёртвых записей, а является настоящим live-set'ом без
  дыр). Возвращает `Result(nodeBytesReclaimed, quadBytesReclaimed)`.

**Важное отличие от `compactQuads`:** компакция узлов **обязательно
переномеровывает** индексы узлов (мёртвые блоки посередине удаляются,
разрыв закрывается). Для `compactQuads` это было намеренно не так
(индексы узлов оставались нетронутыми) — но для узлов это безопасно,
потому что нигде в кодовой базе индекс узла не хранится живым между
разными чтениями кэша: GPU-стриминг (этап 4) всегда потребляет кэш
целиком (`LodPipeline.cachedAtStartup()`/`reloadCache()`) и перезаливает
буферы заново — он никогда не запоминает "узел 42" между отдельными
чтениями. Если в будущем (этап 5, инкрементальный патчинг GPU-буферов)
появятся долгоживущие внешние ссылки на индексы узлов — это поведение
придётся пересмотреть, задокументировано в javadoc класса.

### Подключение к жизненному циклу

`ExceptionalVisionClient.stopPipeline()` (вызывается на
`LevelEvent.Unload`, т.е. при выходе из измерения/мира) теперь
запускает `CacheCompactor.compact(...)` **в отдельном демон-потоке**
после того, как `LodPipeline.close()` дождался завершения фоновых
записей — компакция не должна гоняться с ещё пишущим `LodCacheWriter`
за одни и те же файлы, и не должна блокировать рендер-поток (полное
чтение/перезапись `nodes.bin`/`quads.bin` на большом долгоживущем мире
может занять заметное время).

**Отдельно учтена узкая гонка:** быстрый повторный вход в **то же**
измерение (например, шагнуть в портал и сразу обратно) мог бы начать
новый `LodPipeline.start()` (который читает `nodes.bin`/`index.json`)
пока фоновый поток компакции ещё дописывает те же файлы. Решено через
`ExceptionalVisionClient.COMPACTIONS_IN_PROGRESS` — карта
"dimensionId → CountDownLatch активной компакции"; `startPipeline`
дожидается (`awaitAnyInProgressCompaction`) любой компакции того же
измерения перед стартом. В подавляющем большинстве случаев (обычная
смена измерения, не тот же самый) карта пуста и это no-op без
задержки. Разные измерения никогда не делят директорию кэша, так что
это никогда не блокирует смену измерения на другое.

### Тестирование

**Прогнан юнит-тест** (не сохранён в репозитории — как и остальные
тесты этапов 1-3, одноразовый скрипт против самодельного, но
функционально корректного мини-JSON-парсера, воспроизводящего
поведение Gson для схемы `CacheIndex`): синтетический сценарий —
регион `(0,0)` записан `appendRegion` дважды с разным числом узлов
(2 → 3, как в реальном Баге 6/7), регион `(1,0)` записан один раз (1
узел). Проверено:
- `compactNodes` реально уменьшает `nodes.bin` (6 физических записей →
  4 живых);
- `index.json` после компакции по-прежнему находит оба региона с
  правильным (новым) `nodeCount`;
- новые диапазоны узлов не пересекаются и не выходят за границы файла;
- `compactQuads`, запущенный сразу после `compactNodes` на уже
  прибранном `nodes.bin`, отрабатывает без ошибок;
- повторный запуск `compactNodes` на уже прибранном кэше — **идемпотентен**,
  реклеймит 0 байт (ничего лишнего не трогает, если и так уже компактно).

**Не проверено реальным прогоном в игре** (тот же общий caveat, что и
для остального кода этой сессии — нет доступа к живому Minecraft-
процессу здесь) — в частности, стоит подтвердить на практике: (1) что
`LevelEvent.Unload` действительно даёт достаточно времени фоновому
потоку компакции доработать до следующего события, которое могло бы
удалить файл мира целиком (маловероятно, но не проверено), и (2) что
`awaitAnyInProgressCompaction`'s блокировка рендер-потока при быстром
повторном входе в то же измерение не создаёт заметного фриза на очень
большом кэше (в теории не хуже, чем уже существующее блокирующее
чтение в `LodPipeline.start()` на том же потоке, но не измерено).

### ⚠️ Баг 9 (найден пользователем, НЕ ИСПРАВЛЕН) — после этой сессии LOD рисуется рваными полосами с дырами вместо сплошной поверхности

**Симптом (скриншоты пользователя):** геометрия LOD на удалении — узкие
рваные диагональные ленты рельефа с дырами (видно небо) между ними,
регулярный полосатый паттерн, не похоже на случайный шум по данным.
**Подтверждено пользователем как новая регрессия** — до этой сессии
LOD рисовался сплошным, без полос.

**Статус расследования — причина НЕ найдена и НЕ исправлена**, только
собраны факты и проверены/отклонены гипотезы:

- Лог пользователя (`New World (42)` — многократно пересоздаваемый
  тестовый мир, судя по номеру) показывает сессию, где кэш **уже
  существовал** на момент старта (`16 regions up to date` в самой
  первой строке загрузки) — значит регрессия могла произойти в
  предыдущей, не залогированной здесь сессии (например, при
  компакции, отработавшей на выходе из мира тогда), а не в этой.
- **В этой конкретной сессии компактор не срабатывал** — ни одной
  строки `compacted LOD cache for ...` в логе, хотя игрок вышел из
  мира штатно (`Over left the game` → `Stopping server` → чистое
  `Stopping!` без крашей, ~11 секунд между выходом и остановкой JVM —
  достаточно для фонового потока компакции). Это ожидаемо: в этой
  сессии ни один регион не переобрабатывался с изменившимся числом
  узлов (см. лог — все `appendRegion` были для ранее необработанных
  регионов, все повторные обработки шли через `patchRegion`), значит
  физически нечего было компактировать. Значит эта сессия сама по
  себе не может быть источником регрессии — если она и произошла из-за
  `CacheCompactor`, то в более ранней, не приложенной сессии.
- **Отклонено:** переномерация индексов узлов между сессиями сама по
  себе не должна ломать рендер — GPU-буферы всегда перезаливаются
  целиком при загрузке кэша (`upload`/`uploadIncremental`+
  `nodeCapacityBytes == 0` fallback), индексы согласованы внутри
  одной загрузки, шейдеры не хранят индексы между кадрами.
- **Отклонено:** рассинхрон между `CacheCompactor.compactNodes` и
  `LodCacheData.filterToLiveRanges` — оба используют идентичный
  алгоритм (сортировка `regionsProcessed` по `nodeIndexStart`, склейка
  диапазонов без пропусков), так что уже скомпактированный
  `nodes.bin` для `filterToLiveRanges` — корректный no-op, не должен
  давать иной результат, чем нескомпактированный.
- **ПРОВЕРЕНО В ЭТОЙ СЕССИИ (реальным прогоном, не рассуждением) —
  гипотеза `nodeCount == 0` / `duplicate nodeIndexStart` через
  штатный путь записи ОТКЛОНЕНА:** написан исполняемый репро-тест
  (`Bug9Repro.java`, воспроизведён локально вне репозитория — см.
  «Как это тестировалось» ниже, в репозиторий не закоммичен, т.к. в
  проекте вообще нет каталога тестов, все юнит-тесты в этой и
  предыдущих сессиях были scratch-файлами), который прогоняет
  **настоящие** `LodCacheWriter.appendRegion`/`patchRegion` (не
  синтетически собранный `index.json`) в сценарии: регион A с
  легитимно нулём узлов обрабатывается первым → регион B с реальными
  узлами физически попадает ровно на тот же `nodeIndexStart`, что
  "занимал" бы A (это и есть коллизия из гипотезы) → оба региона
  затем переобрабатываются (`A`: 0→5 узлов через `appendRegion`, `B`:
  40→50 через `appendRegion`, `C`: 25→25 через `patchRegion`) →
  `CacheCompactor.compactNodes`. Каждый узел был помечен уникальным
  тегом (regionId + порядковый номер, закодирован в `lodLevel`), что
  позволило проверить после компакции: **все узлы каждого региона
  остались корректными, ни один не потерялся и не перепутался с
  чужим.** Причина, почему коллизия безопасна: `bulkAppendNodes`
  возвращает `baseIndex = readNodeCount(...)` **до** записи, и при
  `nodeCount == 0` физически ничего не пишет — значит `nodeIndexStart`
  региона с нулём узлов всегда равен текущему общему count **на
  момент его обработки**, а у любого последующего "реального" региона
  с тем же стартовым индексом count к этому моменту ещё не мог
  измениться (не убывает никогда, кроме самой компакции). Из этого
  следует, что коллизирующая нулевая запись **всегда** идёт раньше
  реальной в порядке вставки `regionsProcessed` (`LinkedHashMap`
  сохраняет порядок первой вставки ключа) — а `orderedEntries.sort(...)`
  в Java (`TimSort`) **стабилен**, то есть при равенстве
  `nodeIndexStart` сохраняет именно этот порядок вставки. Ноль-узловая
  запись в паре всегда "выигрывает" сортировку и корректно ничего не
  копирует, реальная — корректно копирует свой блок. **Вывод:** этот
  конкретный путь к багу закрыт, дальше расследование не в эту
  сторону.
- **Побочная находка той же сессии — реальная (но пока не
  подтверждённая как достижимая в проде) дыра в `compactNodes`:**
  отдельный тест (`Bug9CorruptIndexRepro.java`) вручную собрал
  `index.json` с двумя **ненулевыми** живыми записями, указывающими на
  один и тот же `nodeIndexStart` (сценарий, который — в отличие от
  случая выше — текущий `LodCacheWriter` штатно произвести не может,
  т.к. `appendRegion`/`patchRegion` монотонны; но теоретически
  достижим при ручной правке `index.json`, восстановлении из бэкапа,
  или баге в более раннем/удалённом коде, которого сейчас нет в
  репозитории). Результат: **тихая порча данных подтверждена** — один
  из двух регионов получил после компакции узлы, скопированные с
  чужого физического диапазона (полное совпадение тегов с другим
  регионом вместо своих). `compactNodes` не бросает исключение и не
  логирует warning в этом случае — выглядит как успешная компакция.
  **Это не объясняет Баг 9** (т.к. штатный писатель не может создать
  такую запись — см. пункт выше), но это реальная хрупкость, стоящую
  закрыть отдельно: добавить в `compactNodes` явную проверку на
  пересечение диапазонов `regionsProcessed` перед копированием и
  бросать/логировать ошибку вместо тихого дублирования. Не сделано в
  этой сессии — не относится к приоритету №1 (Баг 9), но отмечено как
  дальнейшее улучшение в «Известных пробелах».
- **Из этого следует:** причина Бага 9 всё ещё не найдена, но версия
  про `compactNodes`/legacy nodeCount==0 ослаблена — реальным прогоном
  показано, что этот конкретный механизм не мог быть причиной при
  нормальной работе писателя. Следующей сессии стоит **понизить
  приоритет гипотезы про компактор** (если только не найдётся способ,
  которым `regionsProcessed` мог получить дублирующиеся ненулевые
  диапазоны в первую очередь — что само по себе означало бы более
  серьёзный баг в `appendRegion`/`patchRegion`/`mergeIndexEntry`,
  которого пока не видно) и **сместить фокус на этап 4** (GPU-сторона:
  `quad_cull.comp`'s дистанционные LOD-полосы, `lod_quad.vert`'s
  генерация геометрии по `gl_VertexID`/`gl_DrawID`, любая
  stride/индексация в SSBO) — рваный диагональный полосатый паттерн
  визуально больше похож на баг шага/индексации при выборке LOD-уровня
  или генерации квадов, чем на порчу диска. Пункт 3 из «Что нужно для
  дальнейшего расследования» ниже (удалить кэш и проверить на чистых
  данных) остаётся самым быстрым способом подтвердить это разделение
  и должен идти первым в следующей сессии.
- **РЕШЕНО в этой сессии (по реальным логам пользователя,
  `latest.log`/`debug.log` с его Windows-машины):** подключка в
  `stopPipeline` действительно физически запускалась — дважды, оба
  раза сразу после выхода игрока из мира (`Over left the game` →
  доля секунды спустя `exceptional-vision-cache-compactor` стартует)
  — но **оба раза падала** с одной и той же ошибкой:
  `java.nio.file.AccessDeniedException:
  ...\lodcache\minecraft_overworld\nodes.bin.tmp -> ...\nodes.bin`.
  Причина найдена: `CacheCompactor.readAllNodes`/`compactQuads`
  читали `nodes.bin`/`quads.bin` через `CacheIo.mapReadOnly`
  (`MappedByteBuffer`) и **в той же самой операции компакции** тут же
  пытались атомарно заменить **тот же файл** через
  `CacheIo.writeAtomic`'s `Files.move(..., REPLACE_EXISTING)`. На
  Windows ОС не даёт заменить файл, пока жива его memory-mapping, а
  `MappedByteBuffer` в Java не отмаплен явно нигде в публичном API —
  ОС держит хендл, пока JVM не соберёт буфер сборщиком мусора, что
  почти никогда не успевает произойти за миллисекунды между чтением и
  попыткой замены. На Linux (где до сих пор шло тестирование этой и
  предыдущих сессий) `rename`/`move` поверх замапленного файла
  разрешён ОС — поэтому баг ни разу не проявился в песочнице, только
  на реальной Windows-машине игрока. **Исправлено:** `readAllNodes` и
  чтение `quads.bin` в `compactQuads` переведены на обычный
  `Files.readAllBytes` вместо `mapReadOnly` — файл полностью читается
  в память и хендл закрывается до попытки атомарной замены, мэппинга
  не остаётся. Оба существующих исполняемых теста (`Bug9Repro.java`,
  `Bug9CorruptIndexRepro.java`) перепрогнаны против исправленной
  версии — поведение не изменилось (что и ожидалось: фикс лишь меняет
  способ чтения байтов, не логику компакции).
- **Важное следствие для Бага 9:** раз компакция **ни разу не
  срабатывала успешно** на машине игрока (до этого фикса) —
  `nodes.bin`/`index.json` там **никогда физически не переписывались**
  компактором, что ещё сильнее (уже не только логически, а по
  фактическим логам с этой самой Windows-машины) снимает
  `CacheCompactor` с подозреваемых по Багу 9. Ошибка компакции сама по
  себе безопасна для данных — исключение бросается до того, как
  `compactedIndex.writeTo(indexFile)` успевает выполниться (см.
  `compactNodes`, строка с `writeCompactedNodes` идёт раньше и
  бросает раньше), так что `nodes.bin`/`index.json` остаются
  нетронутыми при каждом падении; комментарий в
  `ExceptionalVisionClient.runCompactionOffThread` («until the next
  successful compaction, exactly as if this run never happened») это
  подтверждает и для этого сценария тоже.
- **Отдельно стоит попросить у пользователя более свежий лог после
  этого фикса** — теперь компакция должна первый раз в истории этого
  мира реально сработать (залогирует `compacted LOD cache for ...
  bytes reclaimed`), стоит убедиться, что полосатый паттерн, если он
  всё ещё есть, не появляется/не меняется именно в этот момент — это
  было бы неожиданно (учитывая всё вышеописанное расследование), но
  стоит перепроверить эмпирически, а не только теоретически.

- **ПОДТВЕРЖДЕНО следующей сессией (свежие `latest.log`/`debug.log`,
  тот же мир `New World (42)`, та же Windows-машина):** фикс
  `AccessDeniedException` реально работает — компактор впервые в
  истории этого мира отработал **успешно**, без единого исключения:
  `Compacted LOD node cache for minecraft:overworld: 40392 -> 17859
  nodes (1081584 bytes reclaimed)`, следом `Compacted LOD quad cache
  ...: 7325100 -> 1440900 quads (47073600 bytes reclaimed)`. Выход из
  игры чистый (`Stopping server` → `Stopping!`, без крашей), компактор
  успел полностью отработать в фоновом потоке за ~9 секунд между
  логаутом и остановкой JVM. Масштаб реклейма (40392→17859 узлов)
  показывает, что на диске за предыдущие сессии реально скопилось
  много мусора от `appendRegion`, который раньше физически не мог
  быть убран.
  - Регионы, зафейлившиеся с `EOFException` в этой сессии (`r.-2.-3`,
    `r.-1.-3`, `r.-2.2`, `r.-3.-3`, `r.-4.-3`, `r.-3.2`, `r.-4.2`,
    `r.-5.2`, `r.-6.*`, `r.0.*`, `r.-1.2`) — ожидаемое поведение из
    фикса Бага 6 (сторонний прегенератор ещё не догенерировал эту
    территорию), не относится к Багу 9.
  - **Визуальный вопрос закрыт следующим сообщением того же
    расследования — скриншот от игрока после этой сессии.**
    Полосатый паттерн **всё ещё присутствует**: на скриншоте видны
    узкие рваные диагональные светлые полосы/царапины, идущие через
    весь рельеф регулярным повторяющимся паттерном, плюс тёмные
    "конфетти"-пятна разбросаны по траве — визуально соответствует
    исходному описанию Бага 9 (не похоже на случайный шум по данным).
  - **Вывод: версия про `CacheCompactor`/`nodes.bin` закрыта
    окончательно.** Раньше это было ослаблено логически (штатный
    писатель не может создать ломающие данные) и по факту (компактор
    у игрока ни разу не срабатывал успешно). Теперь у нас есть прямое
    эмпирическое опровержение: компактор в этой сессии сработал
    **впервые за всю историю мира** (см. выше, 40392→17859 узлов
    физически переписаны на диске) — и полосы никуда не делись.
    Причина Бага 9 гарантированно не в компакторе и не в физическом
    состоянии `nodes.bin`/`quads.bin` на диске.
  - **Приоритет следующей сессии — исключительно этап 4** (GPU-
    сторона): `quad_cull.comp` (политика выбора LOD-уровня по
    дистанции, `baseLodDistance * 2^level`, atomicAdd/indirect draw
    commands), `lod_quad.vert` (генерация геометрии по
    `gl_VertexID`/`gl_DrawID`, stride/индексация в SSBO). Регулярный
    диагональный полосатый паттерн с дырами — классический симптом
    ошибки на границе выбора LOD-уровня или неверного шага/stride при
    чтении `QuadBuffer`/`NodeBuffer`, а не порчи данных. Также стоит
    свежим взглядом перепроверить `FrustumExtractor` (Баг 2 чинил
    похожий по симптомам, но другой по механике артефакт) — но начать
    с самого вероятного: пошаговая сверка индексации в
    `quad_cull.comp`/`lod_quad.vert` построчно против `PackedQuad`/
    `GreedyMesher`, теперь уже под подозрением на off-by-one или
    неверный stride, а не на смысловую логику упаковки (та уже была
    сверена и подтверждена ранее).

### Построчная сверка GPU-пайплайна (следующая сессия, статический анализ)

Раз причина Бага 9 гарантированно не в компакторе (см. выше), эта сессия
построчно сверила весь путь этапа 4 без доступа к живому GL-контексту:
`quad_cull.comp`, `lod_quad.vert`, `LodGpuPipeline.java`,
`LodGpuBuffers.java`, `FrustumExtractor.java` — против источников данных
`NodeData.java`, `PackedQuad.java`, `GreedyMesher.java`, `LodBuilder.java`,
`LodNode.java`.

**Проверено и подтверждено корректным** (полное совпадение конвенций):
- `NodeData` — 48 байт, layout Java/GLSL побайтово идентичен.
- `first = quadOffset*6u` / `gl_VertexID` — фикс Бага 1 не регрессировал.
- localX/localZ/width/height для top-face и `±X`/`±Z`-стен — единицы
  измерения (ячейки vs блоки), направление "fixed"-координаты и
  "runCoordStart" совпадают между `GreedyMesher`/`lod_quad.vert` дословно.
- `worldX = minWorldX + localX * 2^level` — `LodNode.chunkSpan` и
  `cellSize` в шейдере согласованы для любого уровня.
- `FrustumExtractor` (Gribb/Hartmann через `getRow`) и camera-relative
  AABB (фикс Бага 2) — без регрессий.
- `glMultiDrawArraysIndirect(..., stride=0)` с плотно упакованной
  16-байтной структурой — корректно.
- Диспатч compute `(nodeCount+63)/64` — корректный ceil-div, пропущенных
  инвокаций нет.

**Единственное найденное расхождение (не объясняет баг, но стоит
захардить):** `PackedQuad.java` документирует `axis: 3=-Y`, но
`lod_quad.vert` не обрабатывает `axis==3u` отдельной веткой — он
проваливается в `else` (Z-стена) вместе с 4/5. Проверено: `GreedyMesher`
никогда не эмитит `AXIS_NEG_Y` (нижних граней в мешере нет вообще) —
значит это мёртвый код в шейдере, а не причина Бага 9. Рекомендация на
будущее: добавить явную (пусть и недостижимую сейчас) ветку/ассерт для
`axis==3u`, чтобы не превратиться в реальный баг, если кто-то добавит
нижние грани позже.

### РЕЗУЛЬТАТ ДИАГНОСТИКИ (следующая сессия): причина локализована

Игрок прогнал патч из п.2 выше (`lodLevelAppropriate` временно всегда
возвращает `true`, LOD-баннинг по дистанции отключён, рисуются все
узлы всех уровней одновременно) и прислал свежий скриншот + лог.

**Результат: диагональные полосы-дыры в большинстве своём исчезли.**
Цитата игрока: "разрезы между чанками исчезли, бывают редкие чёрные
артефакты, но не страшно; есть небольшое визуальное раздражение из-за
того, что некоторые текстуры налезают друг на друга". Последнее —
ожидаемый побочный эффект самого диагностического патча (несколько
уровней рисуются друг поверх друга одновременно, естественный
z-fighting/овердроу), не самостоятельная проблема.

Лог чистый: `exceptional_vision` инициализировался, обработал регионы
(`Appended`/`Patched`) без единого исключения; `CacheCompactor`
продолжает работать штатно (`21920 -> 19792 nodes`).

**Вывод: причина Бага 9 — в политике выбора LOD-уровня по дистанции
(`lodLevelAppropriate` в `quad_cull.comp`) или в переходах между
соседними уровнями, а НЕ в генерации геометрии конкретного узла.**
Геометрия каждого отдельного узла (top face + стены, PackedQuad,
GreedyMesher) сама по себе, судя по всему, верна — это согласуется с
построчной сверкой кода из предыдущего пункта, которая не нашла
проблем именно в этой части.

Это прямо указывает на давно известный, отдельно задокументированный
пробел («Стены только на внутренних границах узла — швы между узлами
не закрыты, полноценная стыковка это отдельная задача этапа 6», см.
выше) как на вероятную первопричину именно диагонального узора: когда
включён нормальный баннинг по дистанции, соседние узлы на границе двух
разных LOD-уровней имеют разный `cellSize` и не стыкуются швом — при
дистанционном отборе эти нестыкованные границы становятся видимыми
дырами. Из-за того, что кванты дистанции накладываются на квадратную
чанковую сетку, граница между уровнями получается не окружностью, а
рваной ступенчатой/диагональной линией — что и совпадает с описанием
"рваные диагональные ленты с дырами".

**Приоритет следующей сессии:**
0. ✅ Диагностический патч в `quad_cull.comp` уже откачен игроком
   обратно к оригинальной логике (`lodLevelAppropriate` снова считает
   дистанцию как раньше) — можно сразу переходить к реализации фикса,
   без отдельного шага "сначала откатить".
1. Реализовать стыковку швов между соседними узлами разных LOD-уровней
   (собственно задача этапа 6, которая была отложена как "объёмная") —
   это, по всей видимости, и есть настоящий фикс Бага 9, а не
   отдельная забота на будущее.
2. Промежуточный, более дешёвый вариант, если полноценная стыковка
   не влезает в сессию: расширить/перекрыть AABB узлов соседних
   уровней на 1 ячейку с оверлапом вместо честного шва — грубее, чем
   правильная стыковка, но может визуально закрыть дыры быстрее, ценой
   лёгкого z-fighting на границах (похожего на то, что игрок и так
   сейчас видит от диагностического патча, но локализованного только
   на границах уровней, а не по всей сцене).
   (Первоначальный план из трёх пунктов — RenderDoc-захват / точечный
   репро с отключением баннинга / попутная проверка
   `baseLodDistance`=`lodRenderDistance` — выполнен по пункту 2 в этой же
   сессии, см. «РЕЗУЛЬТАТ ДИАГНОСТИКИ» выше; причина найдена, пункты 1 и 3
   сняты с повестки как более не нужные для локализации причины.)

`cache/`+`world/`+`world/lod/` скомпилированы `javac` (JDK 21,
установлен через `apt-get install openjdk-21-jdk-headless`) вне
дерева репозитория, против минимальных, но **функциональных** (не
пустых заглушек) реализаций `com.google.gson.*`
(`JsonParser`/`JsonObject`/`JsonArray`/`JsonPrimitive`/`JsonWriter`) —
т.к. реальный Gson недоступен офлайн (Maven Central не в списке
разрешённых доменов), а `CacheIndex.readFrom`/`writeTo` используют его
напрямую. Это позволило прогнать **настоящий, некопированный и
неупрощённый** код `LodCacheWriter`/`CacheCompactor`/`CacheIndex`, а не
переписанную вручную модель их поведения. Тестовые файлы (`Bug9Repro.java`,
`Bug9CorruptIndexRepro.java`, гson-заглушки) — scratch, не в
репозитории; при необходимости следующая сессия может их
воспроизвести по описанию выше или попросить их у пользователя из
транскрипта этой сессии.

**Что нужно для дальнейшего расследования:**
1. Файлы `<world>/lodcache/minecraft_overworld/{nodes.bin,index.json}`
   из мира пользователя (`New World (42)`) — **до** пересборки/
   удаления — чтобы проверить их напрямую (валидность диапазонов,
   пересечения, дыры) вместо гадания по логам.
2. Лог **предыдущей** сессии (до приложенной), где компакция могла
   реально сработать — если в нём есть строка `compacted LOD cache
   for ... bytes reclaimed`, это подтвердит, что регрессия — следствие
   именно компакции узлов, а не совпадение по времени.
3. Если оба недоступны — самый быстрый способ изолировать причину:
   удалить `<world>/lodcache/<dimension>/` целиком (полный пересчёт с
   нуля, при котором `compactNodes` физически нечего компактировать
   ещё очень долго) и посмотреть, воспроизводится ли полосатый паттерн
   на свежепостроенном кэше. Если да — баг не в компакторе вообще, а
   где-то в этапе 4 независимо от этой сессии, и версию про компактор
   можно снять.

---

## Связка этапов 1→2→3 — `LodPipeline` (✅, новое)

Файл: `src/main/java/org/ex/exceptionalvision/pipeline/LodPipeline.java`.

Раньше в этом файле было явно написано: "`LodCacheWriter`/`LodBuilder`
пока не связаны друг с другом кодом". Это исправлено. `LodPipeline`:

1. При `start()` — загружает существующий кэш (`LodCacheLoader`),
   сканирует `regionDir`, ставит в очередь **только** регионы, для
   которых `LodCacheWriter.needsRecompute(...) == true` (низкий приоритет).
2. Запускает `WorldDirectoryWatcher` на `regionDir` — живые изменения
   получают более высокий приоритет, чем фоновый бэклог.
3. `RegionImportQueue` → `LodBuilderExecutor::submit` →
   `LodPipeline.onLodBuilt` → решение `appendRegion` vs `patchRegion`
   (через `findRegionEntry` + сравнение количества узлов) → запись в
   кэш → (опционально) колбэк `onRegionCached` наружу — это точка
   интеграции для GPU-буферов этапа 4 (см. ниже).

**Важное ограничение, специально описанное в javadoc класса:**
`LodPipeline.cachedAtStartup()` — это снимок **только на момент
старта**, он не обновляется при поступлении новых регионов (повторный
mmap растущего файла на каждый регион нецелесообразен через голый Java
NIO). Для живых обновлений GPU-буферов используйте `onRegionCached`.

**Не проверено юнит-тестом** (только компиляция против реальных
сигнатур `RegionImportQueue`/`WorldDirectoryWatcher` через
header-stub'ы — см. «Как это тестировалось»). Класс сам по себе тонкий
(только связывает уже протестированные части), но сквозной прогон
живого сценария (например, "регион появился → почему-то не тот путь
appendRegion/patchRegion выбрался") не прогонялся.

---

## Этап 4 — GPU-пайплайн, базовый (⚠️ подключён и прогнан в игре, 4 бага исправлены в этой сессии)

### Что реализовано

Шейдеры: `src/main/resources/assets/exceptional_vision/shaders/lod/`
- `quad_cull.comp` — compute-шейдер: frustum-culling (Gribb/Hartmann,
  positive-vertex тест) + выбор LOD-уровня по дистанции (простая
  политика "полосы дистанции", удваивающиеся по уровню — **временная
  заглушка**, реальный screen-space-error метод — это уже этап 5).
  Пишет compact-список видимых узлов + indirect draw commands +
  атомарный счётчик через `atomicAdd`.
- `lod_quad.vert` — процедурно генерирует геометрию квадов прямо из
  `QuadBuffer`/`NodeBuffer` SSBO (без vertex buffer вообще, через
  `gl_VertexID`/`gl_DrawID`). **Построчно сверен** с битовым layout
  `PackedQuad.java` и конвенцией width/height из `GreedyMesher.java`.
- `lod_quad.frag` — распаковывает цвет материала, сверен с
  `MaterialPalette.computeColor`'s packing.

Java: `src/main/java/org/ex/exceptionalvision/render/`
- `GpuCapabilities.java` — требует **OpenGL 4.6 core** (не 4.3!,
  см. ниже почему). Чистый fallback (`supported()==false`), без крашей.
- `ShaderProgram.java` — компиляция/линковка GLSL с classpath (НЕ через
  `ResourceManager` — не resource-pack-overridable, осознанное
  упрощение).
- `LodGpuBuffers.java` — владеет SSBO (node/quad/material/visible-index/
  indirect-commands/draw-counter). `upload(...)` льёт `LodCacheData`
  буферы напрямую в `glBufferData` — **без перепаковки** (для этого и
  проектировался формат этапа 3 — байт-в-байт совместим с GPU-структурой).
- `FrustumExtractor.java` — 6 плоскостей из view-projection матрицы
  через `Matrix4f.getRow` (специально не через `mCR`-поля JOML — там
  легко перепутать столбец/строку и молча сломать culling, что визуально
  выглядит как "часть мира просто не рисуется", а не как краш).
- `LodGpuPipeline.java` — последовательность кадра: сброс счётчика →
  compute dispatch → `glMemoryBarrier` → **явный readback** счётчика на
  CPU (это и есть "базовый" путь этапа 4 по roadmap, в отличие от
  полностью GPU-резидентного `glMultiDrawArraysIndirectCount` — это
  этап 5) → `glMultiDrawArraysIndirect` с явным count.
- `GlStateBackup.java` — сохраняет/восстанавливает program/VAO/
  depth-test/cull-face/blend вокруг нашего рисования (мы вклиниваемся в
  рендер ванильного Minecraft, нельзя оставлять GL-состояние грязным).
- `LodRenderManager.java` — точка входа в рендер.

Wiring: `src/main/java/org/ex/exceptionalvision/client/ExceptionalVisionClient.java`
- Регистрирует `LodRenderManager` на `NeoForge.EVENT_BUS`, вызывает
  `LodGpuPipeline.init()` в `FMLClientSetupEvent.enqueueWork`, создаёт
  `LodPipeline` на `LevelEvent.Load`/`Unload` (только singleplayer/LAN —
  `Minecraft.getSingleplayerServer() == null` → no-op, у клиента на
  удалённом сервере нет локальных файлов региона). Это тот самый wiring,
  который предыдущая версия этого файла ошибочно описывала как
  отсутствующий — см. «Уточнение» в шапке файла.

### Важное отклонение от спеки: НЕ Mixin, а `RenderLevelStageEvent`

`07_forge_integration.md` предлагает Mixin `@Inject` в хвост
`LevelRenderer#renderLevel`. **Я сознательно отклонился от этого** —
проверил через веб-поиск, что сигнатура этого приватного
обфусцированного метода менялась между версиями 1.21.x, и без
реальной сборки/маппингов угадывать её офлайн — рискованно (тихо
сломается, не будет ошибки компиляции). Вместо этого `LodRenderManager`
подписывается на официальное, документированное событие NeoForge
`RenderLevelStageEvent` (`Stage.AFTER_LEVEL`) — тот же момент кадра,
что и хвостовая инъекция, но через стабильный публичный API. Это
задокументировано в javadoc `LodRenderManager` подробно.

### Важное ограничение платформы: macOS

Требование **OpenGL 4.6 core** (не 4.3) — потому что вершинный шейдер
использует core `gl_DrawID` (без этого core-билтина пришлось бы тащить
`GL_ARB_shader_draw_parameters` и `#extension` прагму). Но macOS
устарел на OpenGL 4.1 (Apple задепрекейтил OpenGL целиком в пользу
Metal) — **на macOS весь этот пайплайн в принципе не заработает через
нативный OpenGL**, ни при каком ослаблении требований (compute-шейдеров
там нет вообще, начиная с версии 4.3). `GpuCapabilities.supported()`
корректно вернёт `false` на macOS — это ожидаемо, не баг.

### Что НЕ сделано (важно!)

1. ~~Ничего из этого не подключено к `ExceptionalVision.java`~~ —
   **уже было сделано** до этой сессии, через `ExceptionalVisionClient`
   (см. выше). Предыдущая версия этого файла ошибалась на этот счёт.
2. **Впервые прогнано в реальном LWJGL/NeoForge в этой сессии** — и
   сразу нашлось 4 реальных бага, все исправлены (см. «Сессия — первый
   реальный прогон» ниже). Компиляция раньше шла только против
   рукописных stub-классов (не гарантирует, что сигнатуры СООТВЕТСТВУЮТ
   реальному LWJGL/NeoForge — только что код был внутренне
   непротиворечив относительно предполагаемой сигнатуры) — теперь код
   скомпилирован и прогнан по-настоящему, но **фиксы из этой сессии
   ещё не подтверждены повторным прогоном** (см. «Что делать дальше»).
3. Политика выбора LOD-уровня в `quad_cull.comp` (полосы дистанции,
   `baseLodDistance * 2^level`) — грубая заглушка, не финальная логика.
   Плюс отдельная нерешённая проблема: `ExceptionalVisionClient`
   передаёт в неё конфиг `lodRenderDistance` (2048 по умолчанию)
   напрямую как `baseLodDistance`, из-за чего уровень 0 (полная
   детализация) простирается на все 2048 блоков вместо ширины полосы,
   сравнимой с ванильной дистанцией прогрузки — семантика этого
   параметра не выверена, не трогал без спеки.
4. Нет обработки resize окна / смены GL-контекста (например, при
   переключении в полноэкранный режим) — буферы создаются один раз в
   `init()`, ресайз framebuffer'а сам по себе их не инвалидирует
   (буферы не зависят от размера экрана), но `close()`/повторный
   `init()` при полной пересборке GL-контекста не продуман.

---

## Сессия — первый реальный прогон в игре (4 бага найдены и исправлены)

Пользователь скомпилировал мод и запустил реальную игру — первый раз,
когда этап 4 вообще выполнялся вне компиляции против stub'ов. Ушли в
несколько итераций, каждая со своим симптомом и багом:

### Баг 1 — ничего не рисовалось вообще

**Симптом:** LOD-геометрия не появлялась нигде, никаких ошибок в логе.

**Причина:** двойное смещение в `lod_quad.vert`. `quad_cull.comp`
кодирует `node.quadOffset * 6u` в поле `first` indirect-команды; по
спецификации OpenGL `gl_VertexID` для non-indexed draw уже равен
`first + i`, то есть уже включает это смещение. Шейдер прибавлял
`node.quadOffset` **ещё раз** поверх — `quads[node.quadOffset +
quadIndex]` вместо `quads[quadIndex]`. Для первого узла в буфере
(`quadOffset == 0`) это случайно совпадало, поэтому не было ни ошибки
компиляции, ни краша — просто для всех остальных узлов чтение уходило
за пределы их диапазона квадов (часто и за пределы буфера — на
большинстве драйверов OOB-чтение из `std430` SSBO возвращает нули →
вырожденный квад нулевого размера → невидимая геометрия).

**Фикс:** `lod_quad.vert` — убрано повторное сложение
(`src/main/resources/assets/exceptional_vision/shaders/lod/lod_quad.vert`).

Заодно усилено (не точно доказанная причина, но обоснованное
укрепление): `LodGpuPipeline.runDrawPass` теперь явно выставляет
нужное GL-состояние (`glEnable(GL_DEPTH_TEST)` + `GL_LEQUAL` +
`glDepthMask(true)`, `glDisable(GL_BLEND)`, `glDisable(GL_CULL_FACE)`)
перед рисованием, вместо того чтобы наследовать что осталось от
предыдущего (полупрозрачного) прохода ванильного рендерера на
`Stage.AFTER_LEVEL` — наша процедурная геометрия не гарантирует
единой обмотки граней между верхней гранью узла и стеновыми гранями в
`lod_quad.vert`, так что оставленный culling мог невидимо резать часть
квадов в зависимости от направления взгляда.
`GlStateBackup` заодно научился сохранять/восстанавливать
`glDepthMask` (раньше не трогался вообще, хотя теперь мы его меняем).

### Баг 2 — геометрия появилась, но мерцала при повороте камеры

**Симптом:** после фикса №1 геометрия стала рисоваться, но мерцала —
пропадала и появлялась при повороте/наклоне камеры.

**Причина:** `quad_cull.comp` тестировал AABB узла в **абсолютных
мировых координатах** против плоскостей фрустума, извлечённых из
матрицы `projection * modelView`, где `modelView` — **только поворот,
без трансляции камеры** (так рендерит ванильный Minecraft — всё уже
camera-relative). Значит плоскости фрустума описывают фрустум с
вершиной в мировом origin (0,0,0), а не в реальной позиции камеры.
Проверка мирового AABB против такого фрустума случайно частично
совпадала только пока камера физически близко к origin мира (обычно
спавн) — при повороте то один, то другой узел проходил/не проходил
тест, отсюда и мерцание.

**Фикс:** `quad_cull.comp` — AABB переводится в camera-relative
координаты (`node.aabbMin.xyz - cameraWorldPos`) перед тестом, ровно
так же, как это уже делает `lod_quad.vert` для позиций вершин.

### Баг 3 — дистанция теперь стабильна, но цвета неверные и мерцание не пропало (даже стоя на месте, даже в прогруженных чанках)

**Симптом:** после фикса №2 мерцание от направления взгляда пропало,
но появилось статическое мерцание («будто одна текстура внутри
другой») — не зависящее от движения камеры, с явно неверными цветами
(лиловые/серые пятна), и затрагивающее **вообще всю сцену**, включая
честно прогруженные игроком чанки — не только удалённый LOD.

**Причина — не в рендере, а в данных.** Лог показал: регион
`x=-3, z=0` был обработан в 483 узла (нормально для частично
прогруженного пограничного региона) но **102 277 808 квадов** — у
всех соседних регионов с тем же числом узлов было 20-30 тысяч.

Корень: `GreedyMesher.wallBetween` считал стену между двумя соседними
ячейками, если хотя бы одна присутствует; отсутствующей стороне
присваивалась высота-«часовой» `Integer.MIN_VALUE`. Если реальная
(присутствующая) сторона имела **отрицательную** высоту (нормальная
ситуация ниже уровня моря при лимите высоты -64) —
`remaining = realHeight - Integer.MIN_VALUE` **не переполнялся**
обратно в безобидный отрицательный `int` (как было бы при
неотрицательном `realHeight`), а укладывался в честный положительный
`int` ≈ 2 147 483 584. `emitWallRun` затем послушно нарезал это на
~34 миллиона квадов высотой `MAX_WALL_QUAD_HEIGHT` (63) каждый — с
**одной-единственной границы ячеек** на краю прогруженной территории.

Дальше по цепочке: регион "успешно" (без исключений) дописался в общий
кэш → общий GPU SSBO с квадами раздулся до ~800+ МБ (минимальный
гарантированный спецификацией OpenGL лимит размера SSBO — всего
128 МБ) → буфер либо не влез, либо залился с обрезкой → поскольку это
**один общий буфер на всю карту**, битые данные сломали чтение
геометрии для всех узлов, не только для битого региона — отсюда и
статическое мерцание, и неверные цвета даже в честно прогруженных
чанках, и вероятная связь с последующими
`ClosedByInterruptException`/`RejectedExecutionException` в логе
(скорее всего последствие, не отдельная причина — тяжёлая обработка
раздутого региона могла спровоцировать прерывание пула потоков).

**Фикс:** `GreedyMesher.wallBetween` — если хотя бы одна из двух
соседних ячеек ещё не сгенерирована, стена теперь **не строится вообще**
(`if (!aPresent || !bPresent) return null;`), а не достраивается против
воображаемой высоты `Integer.MIN_VALUE`. Мы всё равно не знаем, что
там на самом деле — состыковка швов между узлами и так отдельно
отложена на этап 6, это согласуется с уже существующей заметкой в
javadoc класса. Плюс защитный потолок
`MAX_SANE_WALL_HEIGHT = 4096` в `emitWallRun` — на случай похожей
ошибки в будущем: деградация до "одна стена обрезана/неверна" вместо
"молча сгенерировать десятки миллионов квадов и убить общий GPU-буфер
для всей карты".

**Важно:** этот фикс не чинит уже испорченный дисковый кэш
задним числом — старые `nodes.bin`/`quads.bin` с этим мусором нужно
удалить (`<world>/lodcache/<dimension>/`), чтобы кэш пересобрался с
исправленным мешером.

### Баг 5 — после фиксов 1-4 геометрия стабильна и не мерцает, но LOD-рельеф накладывается на честно прогруженный ближний рельеф (скриншот пользователя)

**Симптом:** на скриншоте видно смещённый/раздробленный ландшафт прямо
перед игроком — не только на горизонте, где и должен быть LOD, но и в
зоне, которую ванильный Minecraft и так рендерит в полной детализации.

**Причина — то самое известное ограничение из пункта 3 раздела «Что НЕ
сделано» выше, только теперь с подтверждённым визуальным эффектом:**
`ExceptionalVisionClient` передаёт `lodRenderDistance` (по умолчанию
2048 блоков) напрямую как `baseLodDistance` в `quad_cull.comp`. Полоса
уровня 0 — `[0, baseLodDistance)` — то есть узлы уровня 0 (LOD, но
полная сетка колонок) выбираются на любой дистанции от 0 до 2048
блоков **включая место прямо под ногами игрока**, где `Stage.AFTER_LEVEL`
рисует их поверх/вперемешку с уже отрисованной ванильной геометрией
(z-fighting) без какого-либо взаимного согласования — этап 6
(«стыковка с ближним рендером»), который как раз должен был это
решать, не начат вообще.

**Фикс (сужает проблему, не решает этап 6 целиком):** добавлен
`nearCutoffDistance` — узел не рисуется, если ближайшая точка его AABB
к камере ближе этого порога, **независимо от LOD-уровня**. Порог
берётся не из конфига, а из текущего значения ванильной опции
рендер-дистанции (`Minecraft.getInstance().options.getEffectiveRenderDistance()`,
блоков = чанки × 16) плюс запас в 1 чанк, и пересчитывается каждый
кадр в `LodRenderManager` — эта опция меняется в рантайме (меню
настроек), в отличие от `baseLodDistance`/`maxLodLevel`, которые
приходят из мод-конфига и трогаются только при (пере)загрузке.
Затронутые файлы:
- `quad_cull.comp` — новый uniform `nearCutoffDistance`, ранний
  `return` до применения политики LOD-уровня и фрустум-теста.
- `LodGpuPipeline.java` — поле + `setNearCutoffDistance(...)`,
  прокидывается в `runCullPass`.
- `LodRenderManager.java` — читает
  `getEffectiveRenderDistance()` каждый кадр (не кэширует при
  инициализации), вызывает `setNearCutoffDistance` перед
  `renderLodFrame`.

**Это не полноценный этап 6.** Стыков между тем, что видит ванильный
рендерер, и тем, что рисует LOD, по-прежнему нет — просто теперь между
ними есть буфер в 1 чанк, где не рисуется ни то, ни другое (либо,
если ванильная прогрузка чуть отстаёт от `getEffectiveRenderDistance()`
в какой-то момент, узкая полоска без геометрии). Не пытался это
закрывать — полноценная стыковка (шов без дыр и без нахлёста,
согласованный с реальной границей прогруженных чанков, а не с
настройкой) это отдельная, более объёмная задача этапа 6, которую не
стоит наспех делать здесь. Также **не трогал** отдельную нерешённую
семантику `lodRenderDistance` vs `baseLodDistance` (пункт 3 выше) —
этот фикс от неё не зависит и не решает её, оба вопроса всё ещё
открыты.

### Реализована стыковка швов между узлами одного уровня (следующая сессия)

**Что сделано:** `GreedyMesher.wallBetween` рефакторен, чтобы принимать
две (возможно разные) сетки вместо одной — интерьерные вызовы
(`meshWallsAlongX/Z`) передают одну и ту же сетку дважды, как раньше,
без изменения поведения. Добавлены два новых публичных метода —
`meshBoundaryAlongX(west, east)` и `meshBoundaryAlongZ(south, north)` —
которые сравнивают крайний столбец/ряд одной сетки с противоположным
краем соседней и мешат шов той же самой merge-run логикой, что и
внутренние стены. Результат всегда кладётся в локальную систему
координат "восточного"/"северного" узла (`localX=0`/`localZ=0`) — это
единственный валидный способ, так как `PackedQuad` не может ссылаться
на чужой узел.

В `LodBuilder.buildRegion` добавлен проход `stitchLevel(level, width)`
сразу после построения каждого уровня (и `buildLevelZero`, и каждого
`buildNextLevel`), перед подсчётом статистики — для каждой пары
горизонтальных/вертикальных соседей по массиву `level[][]` дозаписывает
граничные квады в `quads` соседнего (восточного/северного) узла.
Соседи одного уровня лежат в этом массиве уже готовыми к моменту
прохода, дополнительный поиск по quadtree не потребовался.

**Проверено не только чтением, но и прогоном** — из этой сессии
скомпилирован (JDK 21, тем же способом, что и в предыдущей сессии с
`CacheCompactor`) реальный, некопированный код `ColumnGrid`/
`PackedQuad`/`GreedyMesher` вне дерева репозитория, и написан
scratch-репро (`StitchRepro.java`, не в репозитории) с 6 кейсами:
- однородный перепад высот на всю ширину границы → 1 склеенный квад
  с ожидаемыми `axis`/`y`/`width`/`height`/`material`;
- тот же перепад, но с разрывом по материалу посередине → 2 отдельных
  квада (merge не должен склеивать разные материалы через границу);
- одинаковая высота по обе стороны → 0 квадов (шва нет);
- **сосед полностью отсутствует** (например, ещё не сгенерированная
  соседняя область) при том что высота присутствующей стороны
  отрицательная (ниже уровня моря) — 0 квадов. Это прицельная проверка
  той самой исторической ловушки с `Integer.MIN_VALUE` (см. комментарий
  в `wallBetween`) — подтверждено, что она не воспроизводится и в
  кросс-нодовом сценарии;
- граница по Z (`meshBoundaryAlongZ`) отдельно, с "северным" соседом
  выше — ожидаемый `axis=NEG_Z` подтверждён;
- обычный `mesh()` одного узла без соседей — не изменился (3 квада:
  2 верхних + 1 внутренняя стена), т.е. рефакторинг `wallBetween` не
  сломал существующее поведение.

Все 6 кейсов прошли (`ALL CHECKS PASSED`). `LodBuilder.java` тоже
скомпилирован тем же вне-репозиторным способом; единственные ошибки —
про неподключённые внешние классы (`BlockState`, `ChunkHeightData`,
`RegionCoordinate`, `RegionResult`), ни одна не в новом коде
(`stitchLevel`/`meshBoundaryAlongX`/`east.quads.addAll(...)`).

**Область действия фикса — явно ограничена, и это может быть не весь
Баг 9:**
- Закрываются швы только между узлами **одного и того же LOD-уровня**,
  лежащими **в одном и том же регионе** (у `LodBuilder.buildRegion`
  нет доступа к сетке соседнего региона — это отдельная, более крупная
  задача, требующая либо построения соседних регионов вместе, либо
  двухпроходного алгоритма с кэшированием граничных столбцов).
- Швы на границе **между разными LOD-уровнями** (там, где сходятся
  дистанционные полосы разной детализации) этим фиксом **не
  закрываются** — сосед другого уровня физически лежит в другом
  `level[][]`-массиве с другим `cellSize`, и эта задача сложнее
  (нужна либо явная стыковка с понижением/повышением разрешения на
  границе, либо дешёвый оверлап AABB, который раньше обсуждался как
  временная мера).
- Раз диагностика показала, что "в большинстве своём" полосы исчезают
  при отключении LOD-баннинга (т.е. когда рисуются все уровни
  одновременно, взаимно перекрывая дыры), а самих одноуровневых узлов
  в обычной сцене примерно на порядок больше, чем границ между
  уровнями — есть основания ожидать, что этот фикс закроет **большую
  часть** визуальной регрессии, но не гарантированно всю.

**Не проверено повторным прогоном в игре** — как и все GPU-фиксы
предыдущих сессий, это изменение сделано без доступа к реальному
GL-контексту. Более того, эта правка меняет **мировые данные**
(`nodes.bin`/`quads.bin` через `LodBuilder`→`LodCacheWriter`), а не
только шейдеры — значит для проверки понадобится либо удалить старый
кэш (`<world>/lodcache/<dimension>/`), либо дождаться, пока
`CacheCompactor`/дозагрузка регионов перестроят задетые узлы, иначе
на диске останутся старые несшитые квады вперемешку с новыми.

### ПОДТВЕРЖДЕНО В ИГРЕ (следующая сессия) — Баг 9 визуально закрыт

Игрок собрал JAR с обоими файлами (`GreedyMesher.java`/
`LodBuilder.java`), создал **новый мир** (чистый кэш по построению,
условие из п.1 выше выполнено автоматически), включил прогрузку 32
чанков, затем переключил на 7 и перезашёл — специально стресс-тестируя
стыковку на неравномерной догрузке регионов в разное время. Прислал 3
скриншота и `latest.log`/`debug.log`.

**Логи чистые:** 36 регионов обработано (`Appended`/`Patched`), два
успешных прогона `CacheCompactor` (один после начальной прогрузки на
32 чанка, другой после перезахода с 7), ни одного исключения от
`exceptional_vision`, штатный `Stopping!` в конце.

**На скриншотах — ни на одном не видно рваной диагональной
полосатости/дыр**, которая была фирменным симптомом Бага 9. Тонкие
вертикальные светлые "иголки" на двух скриншотах — это упрощённая
LOD-геометрия травы/сухих кустов (растительность), не имеет отношения
к Багу 9 и к этой стыковке швов — отдельный, гораздо менее серьёзный
визуальный нюанс мелких объектов на расстоянии.

**Вывод: фикс подтверждён в игре, Баг 9 в практическом смысле закрыт**
(стыковка одноуровневых швов закрыла регрессию полностью или почти
полностью — визуально неотличимо от полного устранения на
предоставленных скриншотах). Формально теоретическая дыра остаётся —
швы на границах МЕЖДУ разными LOD-уровнями и швы на границах МЕЖДУ
регионами всё ещё не стыкуются (см. ограничения выше) — эти сценарии
просто не были явно зафиксированы на присланных скриншотах (игрок не
специально проверял границу дистанционных полос). Если в будущем
всплывут остаточные полосы конкретно на переходе между уровнями
детализации — это тот самый оставшийся, более сложный кусок работы, а
не повод подозревать что-то новое.

### Статус на конец сессии (стыковка швов) — ОБНОВЛЕНО, Баг 9 закрыт

Фикс стыковки внесён, прошёл автономную проверку и **подтверждён в
игре**. Баг 9 снимается с позиции приоритета №1.

**Следующая по важности незакрытая часть этапа 4** (см. «Что делать
дальше» и таблицу статусов выше) — **семантика `baseLodDistance`
vs `lodRenderDistance`**: сейчас `baseLodDistance` в
`quad_cull.comp` берётся напрямую из конфига `lodRenderDistance`
(по умолчанию 2048 блоков) без отдельного смысла. Из-за этого
уровень 0 (самая высокая детализация) растягивается на все 2048
блоков без верхней границы дропа, а `maxLodLevel` не имеет
разумного способа получить собственную, отдельно настраиваемую
дистанцию перехода — это не баг с точки зрения корректности геометрии
(как был Баг 9), а нерешённый вопрос **производительности и
визуального баланса**: сейчас либо непропорционально дорого рисуется
слишком много геометрии на уровне 0, либо (при уменьшении
`lodRenderDistance`) слишком рано включается грубый LOD. Нужно развести
эти два конфига на два независимых значения (`baseLodDistance` —
дистанция первого перехода уровня, `lodRenderDistance` — общая
дальность прорисовки LOD целиком) и прокинуть новое значение через
`LodGpuPipeline`/юниформы в `quad_cull.comp`.

После этого — как и было запланировано — можно считать этап 4
стабильным и переходить к этапу 5 (полный GPU-driven путь).

### Статус на конец сессии

Все 5 фиксов внесены в код (баги 1-4 из предыдущей сессии + баг 5 из
этой). **Повторный прогон в игре после удаления старого кэша ещё не
подтверждён** — следующий шаг для того, кто продолжит эту работу
(человек или нейросеть), см. «Что делать дальше».

### Баг 6 — почти все файлы регионов навсегда пропускались («плавающий остров», нет геометрии рядом со спавном)

**Симптом (новые скриншоты + логи):** игрок стоял на месте — вообще
ничего не отрисовалось. Отлетев в сторону, увидел единственный
маленький оторванный «остров» LOD-геометрии, висящий высоко в
воздухе, с формой рельефа похожей на настоящую, но с совершенно
неверными цветами материалов.

**Причина — не в рендере, а в чтении мира.** Пользователь использовал
отдельный мод-прегенератор чанков, из-за чего десятки файлов
`region/*.mca` создавались/дописывались на диске одновременно с тем,
как `RegionImportQueue` пыталась их читать. Лог показывает, что
**почти каждый регион** (кроме одного) падал с
`java.io.EOFException: Unexpected end of region file at position 0` —
классический признак чтения файла, который уже создан, но ещё не
дописан (0 байт на момент открытия). `RegionImportQueue.processTask`
на любой `IOException` просто логировал `WARN` и **навсегда бросал
задачу** — повторной попытки не было предусмотрено вообще, а
`WorldDirectoryWatcher` может не поймать момент, когда файл наконец
дописан (особенно если прегенератор не трогает файл повторно после
финальной записи в понятном для watcher’а смысле). В итоге за всю
сессию из ~25 запрошенных регионов (радиус 32 чанка) успешно
прочитался **один**, да и то дважды подряд (см. ниже).

Единственный успешно прочитанный регион (`x=-1, z=0`) обработался
**дважды** с разным числом узлов (75, затем 93) — потому что чанки в
нём генерировались постепенно, и `LodPipeline` оба раза корректно
(по документированной логике) выбрал `appendRegion`, а не
`patchRegion`, так как число узлов между обработками изменилось. Это
само по себе не баг (см. известный пробел про компактор `nodes.bin`
ниже) — но обнажило второй, реальный баг (см. «Баг 7»): «осиротевший»
первый блок из 75 узлов (собранный по недогенерированным чанкам, с
неполными/временными данными о верхнем блоке — отсюда и неверные
цвета) **тоже загружался и рисовался** поверх актуального блока из 93
узлов. Именно наложение этих двух версий одного и того же региона —
на фоне того, что вообще ничего больше вокруг не прогрузилось — и
создавало на скриншоте эффект одинокого «плавающего острова» со
странными цветами.

**Фикс (устраняет первопричину — почему регионы не читались):**
`RegionImportQueue` — `IOException` при чтении файла региона больше не
приводит к безвозвратной потере задачи. Файл ставится в очередь
повторно (до 5 попыток, задержка 750мс между ними, через отдельный
`ScheduledExecutorService`) — если через 5 попыток он всё ещё не
читается, тогда уже логируется финальный `WARN` и задача бросается.
Счётчик попыток на файл сбрасывается при успешном чтении. Это
превращает «файл пойман ровно в момент записи → потерян навсегда» в
«обработан чуть позже, когда допишется» — без изменения приоритетной
логики очереди (ретрай возвращается с тем же приоритетом).

**Не проверено повторным прогоном** — как и остальные фиксы этой и
прошлой сессии.

### Баг 7 — «осиротевшие» узлы от повторного `appendRegion` реально загружались и рисовались (не только теоретический технический долг)

**Причина:** известный пробел «компактируется только `quads.bin`, не
`nodes.bin`» (см. «Известные пробелы» ниже) до этой сессии
рассматривался как чисто теоретический — «редкий случай, не
критично». Лог этой сессии показал, что это не так:
`LodCacheLoader`/`LodCacheData.of` **безусловно** читали весь
физический `nodes.bin` по счётчику из заголовка файла (168 узлов = 75
старых + 93 новых для одного и того же региона), не сверяясь с тем,
какие диапазоны узлов `index.json` считает актуальными. Из-за этого
осиротевшие записи не просто занимали место на диске — они
**реально загружались в GPU-буфер и рисовались**, накладываясь на
актуальную геометрию того же региона (отсюда и визуальный мусор,
несовпадающие цвета — старый блок мог быть собран по чанкам на более
ранней/неполной стадии генерации).

**Фикс:** `LodCacheData.of` — вместо чтения `nodes.bin` целиком,
теперь копирует только те диапазоны узлов, которые перечислены в
`index.regionsProcessed()` (там всегда ровно одна — последняя —
запись на регион, `mergeIndexEntry` заменяет, а не добавляет),
пропуская всё остальное. `quads.bin` не тронут — мёртвые квады
по-прежнему физически остаются в файле (как и раньше, ждут
`CacheCompactor`), но раз ни один живой узел на них больше не
ссылается, они просто не попадают на GPU. Затронутый файл:
`cache/LodCacheData.java` (метод `of`, новый приватный
`filterToLiveRanges`).

**Это не отменяет нужность полноценного компактора `nodes.bin`** (см.
«Известные пробелы») — на диске мусор всё ещё накапливается со
временем на очень долгоживущих мирах; этот фикс только не даёт ему
попадать на экран.

**Не проверено повторным прогоном.**

### Статус на конец этой сессии

Итого 7 багов найдено и исправлено за три сессии (1-4 → 5 → 6-7).
**Ни один из фиксов этой сессии (5, 6, 7) не подтверждён повторным
прогоном** — см. «Что делать дальше».

### Сессия — второй реальный прогон: Баг 6 частично подтверждён, найден Баг 8

Пользователь прогнал тот же мир после фиксов 5-7. Результат:

- **Баг 6 (регионы навсегда терялись) — подтверждён исправленным.**
  Лог показывает 16 успешно обработанных регионов подряд (было: один).
  Оставшиеся неудачи (`r.2.*.mca`, 6 попыток и явный отказ) — это уже
  не баг: та территория ещё не пригенерирована сторонним мод-
  прегенератором, там физически нечего читать. Отсюда и «дальше
  ничего не прорисовывалось» в сообщении пользователя — это ожидаемо:
  мод только читает то, что реально сгенерировано на диске, он не
  умеет (и не должен) синтезировать LOD для непрогруженной земли.
- **Баг 7 (осиротевшие узлы) — не удалось ни подтвердить, ни
  опровергнуть по этим логам** (в этом прогоне ни один регион не
  обрабатывался дважды с изменением числа узлов, так что сценарий бага
  просто не возник).

**Новый баг (Баг 8) — океан рисуется как суша.** Причина найдена и
исправлена: `RegionFileReader.isConsideredOpaque` считает "верхним
непрозрачным блоком" первый блок с `canOcclude() == true`, а у воды
это `false` (сквозь воду видно дно — как и в ванильном рендере) — то
есть скан проходил СКВОЗЬ всю толщу воды и находил дно озера/океана,
записывая его цвет как "верх" колонки. LOD в итоге рисовал морское дно
(песок/гравий) без всякого намёка на то, что там была вода — ровно то,
что видно на скриншоте пользователя (озеро выглядит как часть пляжа).
Комментарий в коде прямо предупреждал об этом как о запланированной,
но не сделанной доработке ("expected to live in the LOD Builder
(stage 2)" — но в `LodBuilder`/`ColumnGrid` такой обработки тоже не
оказалось). **Фикс:** `isConsideredOpaque` теперь считает воду/лаву
(`state.getFluidState()` не пусто) непрозрачной сама по себе — колонка
останавливается на поверхности воды, и LOD рисует плоскую
воду-цветную поверхность вместо дна, как и должно быть для дальнего
LOD. Затронутый файл: `world/RegionFileReader.java`.

**Про "конфетти" — разноцветный цветной шум/фиолетовое пятно на
скриншотах.** Это НЕ похоже на классический баг с индексами/памятью:
кодирование индекса материала (16 бит, до 65536 значений — с большим
запасом даже для тяжёлого модпака), путь палитры (`MaterialPalette` →
GPU SSBO), и упаковка/распаковка в шейдерах (`packed1` биты 12-27) —
всё проверено и совпадает один в один между Java и GLSL, без
переполнений. Более вероятное объяснение: `LodDownsampler` при
объединении 4 дочерних ячеек в одну **всегда берёт самую высокую** (не
усредняет цвет) — это осознанное решение (сохранять силуэты пиков), но
на территории с плотно расположенными разноцветными высокими
объектами (в модпаке есть `alexscaves` — у него встречаются
разноцветные кристальные образования; либо это could быть деревня/
конструкция с крашеными блоками) даёт ровно такой "соль-и-перец"
результат — детерминированно, из настоящих данных, а не мусор. **Не
берусь фиксить это как баг без подтверждения** — не могу сам зайти в
игру и посмотреть, что там реально стоит на земле. Проверить: долететь
до места на скриншоте в ванильном виде (без LOD) и сравнить — если
цвета там ДЕЙСТВИТЕЛЬНО разноцветные (кристаллы/крашеные блоки), это
не баг, просто грубая (для этой стадии) политика даунсемплинга; если
там на самом деле что-то однотонное — тогда дело в чём-то другом, и
стоит прислать координаты/скриншот той же точки в ванильном рендере
для сравнения.

**Не проверено повторным прогоном** (фикс Бага 8).

### Статус на конец этой сессии

Итого 8 багов найдено (7 исправлено и не проверено в игре повторно +
Баг 6 частично подтверждён исправленным реальным прогоном). Баг 7
статус неизвестен (сценарий не возник в этом прогоне). "Конфетти" —
не подтверждено как баг, требует визуального сравнения с ванильным
рендером на месте.

### Сессия — инкрементальная загрузка на GPU (первый пункт списка "что дальше")

Раньше `onRegionCached` на **каждое** обновление хотя бы одного региона
(даже патч на 95 квадов) вызывал `LodGpuBuffers.upload(...)`, который
делал `glBufferData` (полная реаллокация + перезаливка) для буфера
узлов И буфера квадов **целиком**, независимо от того, что реально
изменилось. На реальном прогоне кэш дорос до ~11 тыс. узлов / ~2 млн
квадов (квады — 8 байт каждый, узлы — 48 байт: квады это
подавляющее большинство байт, на порядки больше узлов) — перезаливать
это целиком на каждый маленький патч было прямым источником будущих
фризов на большом мире, тем более на подходе этап 5 с более активным
стримингом.

**Ключевое наблюдение, на котором всё построено:** `quads.bin` по
конструкции **только дополняется** — `LodCacheWriter.appendQuads`
всегда пишет в конец файла, и даже когда регион патчится
(`patchRegion`), его "свежие" квады тоже дописываются в конец, а не
перезаписывают старые (старый диапазон просто становится
осиротевшим — см. Баг 7). Значит буфер квадов на GPU можно всегда
обновлять строго добавлением нового хвоста, без единого исключения.
`nodes.bin`, в отличие от квадов, может **переупорядочиваться** между
загрузками (см. фильтрацию осиротевших узлов, Баг 7) — но узлов на
порядки меньше по объёму, так что перезаливать их целиком каждый раз
всё равно дёшево.

**Реализация (`render/LodGpuBuffers.java`):**
- `upload(...)` — старое поведение переименовано/сохранено как полный
  путь (используется при первой загрузке мира и как safety-fallback).
- Новый `uploadIncremental(...)`:
  - Буфер узлов — по-прежнему перезаливается целиком (`glBufferSubData`
    на 0, без реаллокации, если хватает выделенной ёмкости).
  - Буфер квадов — загружается **только новый хвост**
    (`glBufferSubData` со смещением `oldQuadCount * 8`), с ростом
    выделенной GPU-памяти вдвое при нехватке места через
    `glCopyBufferSubData` (копирование старого содержимого GPU→GPU,
    без обратного чтения на CPU) в новый, больший буфер-объект — старый
    удаляется, ссылки (`bindBufferBase`) берут актуальный id на
    каждый кадр заново, так что подмена объекта прозрачна.
  - Палитра материалов и scratch-буферы (`visibleNodeIndices`,
    indirect commands) — перезаливаются целиком каждый раз (малы,
    оптимизировать не имеет смысла; scratch-буферы и так каждый кадр
    полностью перезаписываются compute-шейдером).
  - Fallback на полный `upload(...)`: первый вызов (нечего наращивать)
    или если `quadCount` вдруг **уменьшился** (кэш пересобрали с нуля
    из-под нас — например, смена мира/измерения после `clear()`, или
    бамп версии формата) — единственный случай, когда "только
    добавление" не гарантировано.
- `clear()` — теперь также сбрасывает `nodeCapacityBytes`/
  `quadCapacityBytes`, а не только счётчики — иначе следующая загрузка
  (например, кэш другого измерения) ошибочно попыталась бы
  диффить против чужих данных.
- `LodGpuPipeline.uploadCache(...)` — теперь вызывает
  `buffers.uploadIncremental(...)` вместо `buffers.upload(...)`;
  публичная сигнатура не изменилась, оба места вызова в
  `ExceptionalVisionClient` трогать не пришлось.

**Не проверено реальным прогоном** — как и всё в этой сессии, нет
доступа к GL-контексту здесь. Особенно стоит проверить на реальном
прогоне: рост буфера квадов при активном исследовании мира (несколько
`ensureQuadCapacity` подряд, с реальным `glCopyBufferSubData`), и
переход между мирами/измерениями (`clear()` → следующая загрузка
обязана пойти по полному пути, не по инкрементальному).

### Что делать дальше

1. Подтвердить инкрементальную загрузку реальным прогоном (см. выше) —
   в идеале с логированием, сколько раз сработал `uploadIncremental`
   инкрементально против скольки раз откатился на полный `upload`.
2. ~~Компактор `nodes.bin`/`quads.bin`~~ — **сделано в этой сессии**,
   см. «Компактор `nodes.bin`/`quads.bin`» выше.
3. Этап 5: нормальный выбор LOD-уровня (screen-space error) и
   сглаживание переходов вместо простых дистанционных полос — это и
   есть источник «низкого качества» картинки, которое видел
   пользователь.
4. Этап 6 полностью (сейчас — только затычка из Бага 5).

---

## Что делать дальше

**Порядок важен:**

0. **⚠️ Баг 9 (см. выше) — приоритет №1, раньше всего остального.**
   Эта сессия дважды ослабила версию про `compactNodes`: реальным
   тестом (штатный `LodCacheWriter` не может произвести данные,
   ломающие компакцию так, как предполагала гипотеза) и — сильнее —
   реальными логами игрока, показавшими, что компактор у него **ни
   разу не срабатывал успешно** (падал с `AccessDeniedException` на
   Windows из-за незаснятого `MappedByteBuffer`; исправлено). Значит
   `nodes.bin`/`index.json` у игрока никогда не переписывались
   компактором — версия про него ослаблена ещё сильнее. Следующий шаг
   всё ещё тот же и самый быстрый: удалить
   `<world>/lodcache/<dimension>/` целиком и проверить, воспроизводится
   ли полосатый паттерн на свежепостроенном кэше (либо получить новый
   лог после фикса компактора и убедиться, что первая успешная
   компакция ничего не меняет). Если полосы всё равно есть —
   переключаться на этап 4 (`quad_cull.comp`, `lod_quad.vert`,
   SSBO-индексация/stride, политика LOD-уровня по дистанции). Если файлы
   `nodes.bin`/`index.json` из воспроизведшего баг мира всё же станут
   доступны — всё равно стоит проверить их на пересекающиеся диапазоны
   `regionsProcessed` (см. отдельную, не связанную с Багом 9 находку
   про хрупкость `compactNodes` при побитом `index.json` — если такой
   `index.json` возник не вручную, а как-то естественно, это было бы
   более серьёзной новостью, чем сам Баг 9).
1. **Подтвердить все 7 фиксов реальным повторным прогоном.** Удалить
   старый (испорченный) диск-кэш — `<world>/lodcache/<dimension>/` —
   пересобрать мод, зайти в тот же мир, дать фоновой обработке
   регионов закончиться (следить за `Appended region .../
   Patched region ...` в логе — квадов на регион должно быть в разумных
   пределах, счётчик из тысяч, не из миллионов; **новое:** также
   проверить, что region-io больше не сыплет непрерывными
   `Failed to read region file` без последующего успеха — единичные
   `DEBUG`-сообщения о повторной попытке нормальны, постоянные `WARN`
   об окончательном отказе — нет), и визуально проверить:
  - геометрия рисуется стабильно (не мерцает) при повороте камеры и
    стоя на месте;
  - цвета материалов соответствуют ожидаемым (не лиловые/серые пятна);
  - совпадает по высоте/положению с honestly-прогруженным ванильным
    рельефом на границе видимости, без явных разрывов;
  - LOD-геометрия не видна и не мерцает в зоне, которую честно
    рендерит сам Minecraft (проверить сменой render distance в
    настройках на лету — порог должен следовать за ней); на самой
    границе может быть тонкая полоса без геометрии ни от одного из
    двух рендереров (см. «Баг 5» — это ожидаемо для этого фикса,
    закрывается только полноценным этапом 6);
  - **новое:** геометрия прогружается вокруг игрока равномерно (не
    только один изолированный «остров» где-то в стороне), и при
    использовании стороннего прегенератора чанков регионы
    появляются в кэше один за другим, а не теряются молча (Баг 6);
  - **новое:** ни один регион не отрисовывается дважды/с наложением
    после того, как его чанки продолжили генерироваться в фоне
    (Баг 7 — проверяется сложнее всего, лучше всего заметно как
    "дублирующиеся, слегка отличающиеся по цвету слои рельефа" в
    районе, который догенерировался у игрока на глазах).
2. Если всё стабильно — заняться пунктом 3 из раздела «Что НЕ
   сделано»: развести смысл `lodRenderDistance` и `baseLodDistance`
   (сейчас конфиг напрямую используется как ширина полосы уровня 0,
   что не то же самое, что "дистанция, на которую LOD должен быть
   виден").
3. После этого — этап 5 (полный GPU-driven путь: убрать CPU-readback,
   `glMultiDrawArraysIndirectCount`, персистентные mapped-буферы для
   live-обновлений вместо `uploadCache` целиком). **Не имеет смысла
   начинать раньше пункта 1** — этап 5 строится поверх того же
   culling-шейдера и того же мешера; неподтверждённые баги там
   переживут ту же миграцию, и станет сложнее их найти среди новой
   логики.
4. Отдельно, не блокирует остальное: компактор `nodes.bin` —
   ~~сделано в этой сессии~~, см. «Компактор `nodes.bin`/`quads.bin`»
   выше; ещё не закрыто — обработка resize/пересборки GL-контекста,
   добавить LICENSE/README перед публикацией.

---

## Известные пробелы / технический долг

1. **Нет реального света** — `LodBuilder.DEFAULT_LIGHT = 15`.
2. **`LodNode.dirty` ни на что не влияет** — нет автоматического
   пересчёта, когда соседние чанки наконец генерируются.
3. **Стены только на внутренних границах узла** — швы между соседними
   LOD-узлами не закрыты (этап 6).
4. **LICENSE и README отсутствуют.**
5. **Нет юнит-теста этапа 1 на реальном мире Chunky.**
6. `MaterialPalette` — без биомного тонирования травы/листвы.
7. ~~**Компактируется только `quads.bin`, не `nodes.bin`**~~ —
   **закрыто в этой сессии**, см. «Компактор `nodes.bin`/`quads.bin`»
   выше. (`CacheCompactor.compactNodes`, подключён к
   `ExceptionalVisionClient.stopPipeline`.)
8. **Весь этап 4 не подключён и не проверен в реальном окружении**
   (см. раздел выше) — это НЕ то же самое, что "готово", несмотря на
   галочку с предупреждением в таблице.
9. Политика выбора LOD-уровня в compute-шейдере — временная заглушка
   (полосы дистанции), не финальная.

---

## Как это тестировалось

**Этапы 1–3 + `LodPipeline`:** офлайн, через самодельные, но
функционально корректные (не заглушки-пустышки) stub-классы Gson и
Minecraft API, плюс header-accurate stub'ы для `RegionImportQueue`/
`WorldDirectoryWatcher` (только публичные сигнатуры, не поведение).
Прогнаны юнит- и интеграционные тесты (не сохранены в репозитории —
одноразовые скрипты): геометрия greedy-меша, downsampling, полная
пирамида на тестовом регионе (1365 узлов), полный цикл кэша (запись →
загрузка → инкремент → компакция → инвалидация по версии формата),
**новое:** `appendRegion`/`patchRegion` с проверкой отсутствия
дублирования узлов при повторной обработке, и сквозной pipeline-тест
(симулированный регион → `LodBuilder` → `LodCacheWriter` →
`LodCacheLoader`, побайтовая проверка round-trip).

**Этап 4 (`render/` пакет):** ТОЛЬКО компиляция против вручную
написанных stub-классов `org.lwjgl.opengl.GL46C`,
`org.lwjgl.opengl.GL`/`GLCapabilities`, `org.lwjgl.system.MemoryUtil`,
`org.joml.{Matrix4f,Vector3f,Vector4f}`,
`net.neoforged.neoforge.client.event.RenderLevelStageEvent`,
`net.minecraft.client.Camera`/`net.minecraft.world.phys.Vec3` — все
сигнатуры в стабах писались ПО ПАМЯТИ и веб-поиску (для нескольких
ключевых: `BlockState#getMapColor`, структура `RenderLevelStageEvent`),
**не сверялись с реальными jar-файлами** (нет доступа к Maven Central
офлайн). Это качественно менее надёжная проверка, чем для этапов 1–3 —
там stub'ы были дополнены исполняемыми тестами с проверкой поведения
(не только компиляции), а этап 4 — только компиляция.

**Первым делом при работе в среде с доступом к интернету/Gradle** —
прогнать `./gradlew build` и `./gradlew runClient`, начиная с пакета
`render/` (самый непроверенный код) и `LodRenderManager`'а
(NeoForge event API мог измениться между версиями).

Если захочешь добавить настоящий JUnit — в `build.gradle` пока нет
тестового source set (`src/test/java` не настроен).

**Компактор `nodes.bin`/`quads.bin` (эта сессия):** офлайн, тем же
подходом что этапы 1-3 — самодельный, но функционально корректный
мини-JSON-парсер (не заглушка, реально парсит/пишет JSON) воспроизводит
`CacheIndex`'s схему. Синтетический сценарий воспроизводит ровно
ситуацию из Бага 6/7 (регион переобработан `appendRegion` с другим
числом узлов, второй регион записан один раз) и проверяет: реальное
уменьшение `nodes.bin`, корректность `index.json` после компакции
(оба региона находятся, правильный `nodeCount`, диапазоны не
пересекаются и не выходят за границы файла), совместимость
`compactQuads` с уже прибранным `nodes.bin`, и идемпотентность
повторного запуска. **Не проверено реальным прогоном в игре** — как
и весь остальной код этой сессии, доступа к живому Minecraft-процессу
здесь нет. Подробности теста — в разделе «Компактор `nodes.bin`/
`quads.bin`» выше.